package proxyBank;

public class client {
	
	   public static void main(String[] args) {
		   ProxyBank image = new ProxyBank();

	      image.getBalance();
	      image.setlogin("logged");
	      
	      image.getBalance();
	      image.withdraw(50);
	      image.deposit(100);	
	   }
	}