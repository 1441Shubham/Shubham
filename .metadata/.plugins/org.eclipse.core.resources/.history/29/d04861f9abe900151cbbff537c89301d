package proxyBank;

public class client {
	
	   public static void main(String[] args) {
		   ProxyImage image = new ProxyImage();

	      //image will be loaded from disk
	      //image.display();
	      image.getBalance();
	      ((ProxyImage) image).setlogin("logged in");
	      System.out.println("");
	      
	      //image will not be loaded from disk
	      //image.display(); 	
	   }
	}